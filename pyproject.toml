[project]
name = "airflow-vs-dagster"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "apache-airflow>=3.1.7",
    "dagster>=1.12.15",
    "dagster-webserver>=1.12.15",
    "httpx<0.28",
]
